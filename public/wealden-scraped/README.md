# Wealden scraped images

Text-based SVG placeholders that mirror the scraped image map. Binary image downloads (JPEG/PNG/WebP) should be kept outside the repo to avoid committing unsupported binary assets.
