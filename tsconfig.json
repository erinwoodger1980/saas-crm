{
  "compilerOptions": {
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "types": ["node"],
    "lib": ["esnext"],

    /* Important: leave these OUT at the root.
       Each package (api/web) will define its own:
       - rootDir / outDir
       - module / target / moduleResolution
       - jsx / plugins / noEmit / allowJs / incremental
       - baseUrl / paths
    }
  },
  "exclude": ["node_modules", "dist", "web/.next", "web/out"]
}