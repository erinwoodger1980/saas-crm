-- Get LAJ Joinery tenant ID
SELECT id, slug, name FROM "Tenant" WHERE slug = 'laj-joinery';

-- Then check what lookup tables exist for this tenant
-- Run the first query to get the ID and substitute below
