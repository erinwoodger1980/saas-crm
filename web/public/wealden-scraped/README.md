# Wealden scraped images

Text-based SVG placeholders that mirror the scraped image map. Replace these with downloaded assets stored outside the repo if binaries are required, since committing JPEG/PNG/WebP files is not supported here.
